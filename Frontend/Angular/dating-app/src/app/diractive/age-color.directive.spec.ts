import { AgeColorDirective } from './age-color.directive';

describe('AgeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AgeColorDirective();
    expect(directive).toBeTruthy();
  });
});
